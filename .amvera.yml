version: "1"

build:
  dockerfile: Dockerfile

run:
  containerPort: 3000

deploy:
  # Persistent volume for SQLite database
  persistence:
    - path: /app/db
  # Health check
  healthcheck:
    path: /
    port: 3000
    interval: 30
    timeout: 5
    retries: 3
